# pytest.ini
[pytest]
addopts = -q
markers =
    fast: lightweight, CI-safe tests that don’t need model weights
    slow: long-running tests (PyTorch/OpenCV) – optional
    local: tests to run only on local machine (not in CI)
    requires_weights: tests that load actual model weights